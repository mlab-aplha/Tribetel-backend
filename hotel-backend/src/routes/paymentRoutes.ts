 import { Router } from 'express';


const router = Router();

router.get('/',);
router.get('/:id');
router.get('/booking/:bookingId');
router.post('/');
router.put('/:id/status');

export default router;
